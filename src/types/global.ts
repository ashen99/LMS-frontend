export interface DataType {
  key: string;
  name: string;
  contact: number;
  address: string;
}

export interface BookDataType {
  key: string;
  title: string;
  author: string;
  isbn: string;
}
